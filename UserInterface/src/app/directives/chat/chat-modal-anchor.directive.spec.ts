import { ChatModalAnchorDirective } from './chat-modal-anchor.directive';

describe('ChatModalAnchorDirective', () => {
  it('should create an instance', () => {
    const directive = new ChatModalAnchorDirective();
    expect(directive).toBeTruthy();
  });
});
